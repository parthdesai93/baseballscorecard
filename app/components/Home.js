var React = require('react');
var GameContainer = require('../containers/GameContainer');

function Home(props) {
  return(
    <div>
      <GameContainer />
    </div>
  );
}

module.exports = Home;
